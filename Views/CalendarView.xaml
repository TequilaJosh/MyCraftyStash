<UserControl x:Class="MyCraftyStash.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MyCraftyStash.ViewModels"
             xmlns:views="clr-namespace:MyCraftyStash.Views"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- Day header style -->
        <Style x:Key="DayHeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0,6"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- ════════════════════════════════════════════════════════════════ -->
        <!-- LEFT: Calendar Grid                                              -->
        <!-- ════════════════════════════════════════════════════════════════ -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Month navigation header -->
            <Border Grid.Row="0" Background="{DynamicResource CardBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="20,14">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource SecondaryButton}" Content="◀"
                                Command="{Binding PreviousMonthCommand}"
                                Width="34" Height="34" Padding="0" FontSize="13"/>
                        <TextBlock Text="{Binding CurrentMonthLabel}"
                                   FontSize="20" FontWeight="Bold"
                                   Foreground="{DynamicResource TextBrush}"
                                   VerticalAlignment="Center" Margin="16,0"/>
                        <Button Style="{StaticResource SecondaryButton}" Content="▶"
                                Command="{Binding NextMonthCommand}"
                                Width="34" Height="34" Padding="0" FontSize="13"/>
                        <Button Style="{StaticResource SecondaryButton}" Content="Today"
                                Command="{Binding GoToTodayCommand}"
                                Padding="12,6" Margin="12,0,0,0"/>
                    </StackPanel>

                    <!-- Loading indicator -->
                    <TextBlock Text="Loading..." Foreground="{DynamicResource TextMutedBrush}"
                               HorizontalAlignment="Right" VerticalAlignment="Center"
                               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"/>
                </Grid>
            </Border>

            <!-- Day-of-week headers -->
            <Grid Grid.Row="1" Background="{DynamicResource CardBrush}"
                  Margin="0,0,0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="SUN" Style="{StaticResource DayHeaderText}"/>
                <TextBlock Grid.Column="1" Text="MON" Style="{StaticResource DayHeaderText}"/>
                <TextBlock Grid.Column="2" Text="TUE" Style="{StaticResource DayHeaderText}"/>
                <TextBlock Grid.Column="3" Text="WED" Style="{StaticResource DayHeaderText}"/>
                <TextBlock Grid.Column="4" Text="THU" Style="{StaticResource DayHeaderText}"/>
                <TextBlock Grid.Column="5" Text="FRI" Style="{StaticResource DayHeaderText}"/>
                <TextBlock Grid.Column="6" Text="SAT" Style="{StaticResource DayHeaderText}"/>
            </Grid>

            <!-- Calendar cells grid (6 rows × 7 cols = 42 cells) -->
            <ItemsControl Grid.Row="2" ItemsSource="{Binding DayCells}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="6" Columns="7"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,1"
                                MinHeight="80" Cursor="Hand">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCurrentMonth}" Value="False">
                                            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource DangerSoftBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsToday}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource WarnSoftBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding DataContext.SelectDayCellCommand, RelativeSource={RelativeSource AncestorType=views:CalendarView}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <Grid Margin="4,4,4,2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Day number -->
                                <Grid Grid.Row="0">
                                    <Border HorizontalAlignment="Left" Width="26" Height="26"
                                            CornerRadius="13">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Date.Day}" FontSize="12" FontWeight="SemiBold"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCurrentMonth}" Value="False">
                                                            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>

                                <!-- Per-cell content stack: TE hours/status, TE events, then user events -->
                                <StackPanel Grid.Row="1" Margin="0,2,0,0">

                                    <!-- TE published store hours (small caption) -->
                                    <TextBlock Text="{Binding TeHours}" FontSize="9"
                                               Foreground="{DynamicResource TextMutedBrush}"
                                               TextTrimming="CharacterEllipsis"
                                               Visibility="{Binding HasTeHours, Converter={StaticResource BoolToVisConverter}}"
                                               Margin="0,0,0,1"/>

                                    <!-- TE status banner (Closed / Open: ...) -->
                                    <Border CornerRadius="3" Padding="4,1" Margin="0,1"
                                            Background="{DynamicResource SidebarSelectedBrush}"
                                            Visibility="{Binding HasTeStatus, Converter={StaticResource BoolToVisConverter}}">
                                        <TextBlock Text="{Binding TeStatus}" FontSize="9"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Border>

                                    <!-- TE-image-derived event chips -->
                                    <ItemsControl ItemsSource="{Binding TeEvents}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="3" Margin="0,1" Padding="4,1"
                                                        Background="#5F7A5C">
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Title}" FontSize="9" Foreground="White"
                                                                   TextTrimming="CharacterEllipsis" FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding Time}" FontSize="9" Foreground="White"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- User-created events + /classes-sourced events -->
                                    <ItemsControl ItemsSource="{Binding Events}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="3" Margin="0,1" Padding="4,1"
                                                        Background="{DynamicResource PrimaryBrush}">
                                                    <TextBlock Text="{Binding Title}" FontSize="10" Foreground="White"
                                                               TextTrimming="CharacterEllipsis"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <!-- ════════════════════════════════════════════════════════════════ -->
        <!-- RIGHT: Day detail + Event form panel                            -->
        <!-- ════════════════════════════════════════════════════════════════ -->
        <Border Grid.Column="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,0,0"
                Background="{DynamicResource SidebarBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
            <Grid Grid.Row="0">

                <!-- Day Events Panel (visible when not editing/adding) -->
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAdding}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Selected day header -->
                    <Border Grid.Row="0" Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="16,12">
                        <Grid>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Select a day"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedDay}" Value="{x:Null}">
                                                    <Setter Property="Text" Value="Select a day"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                    <Run Text="{Binding SelectedDay.Date, Mode=OneWay, StringFormat='dddd, MMMM d', FallbackValue=''}"/>
                                </TextBlock>
                                <TextBlock FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,2,0,0">
                                    <Run Text="{Binding SelectedDayEvents.Count, Mode=OneWay, FallbackValue='0'}"/>
                                    <Run Text=" event(s)"/>
                                </TextBlock>
                            </StackPanel>

                            <Button Content="+ Add Event"
                                    Command="{Binding StartAddCommand}"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Padding="10,6">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedDay}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Status message. Outer Grid lets the events ScrollViewer
                         fill remaining space without pushing into the TE Shop
                         Hours panel below it. -->
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0">

                        <!-- Taylored Expressions info for the selected day (hours,
                             status banner, image-derived events). Only rendered
                             when a day is selected AND it has at least one piece
                             of TE content. The DataContext is the SelectedDay
                             CalendarDayCell so we can bind directly to TeHours
                             etc.; visibility cascades from HasAnyTeContent. -->
                        <Border DataContext="{Binding SelectedDay}"
                                Background="{DynamicResource CardBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1" CornerRadius="8"
                                Padding="12,10" Margin="12,8,12,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasTeHours}"  Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger>
                                        <DataTrigger Binding="{Binding HasTeStatus}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger>
                                        <DataTrigger Binding="{Binding HasTeEvents}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock Text="Taylored Expressions" FontSize="11" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,6"/>

                                <!-- Status banner takes priority — when set, hours line is implied. -->
                                <Border CornerRadius="4" Padding="6,3" Margin="0,0,0,6"
                                        Background="{DynamicResource SidebarSelectedBrush}"
                                        HorizontalAlignment="Left"
                                        Visibility="{Binding HasTeStatus, Converter={StaticResource BoolToVisConverter}}">
                                    <TextBlock Text="{Binding TeStatus}" FontSize="12" FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextBrush}"/>
                                </Border>

                                <!-- Operating hours line -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6"
                                            Visibility="{Binding HasTeHours, Converter={StaticResource BoolToVisConverter}}">
                                    <TextBlock Text="Hours: " FontSize="12"
                                               Foreground="{DynamicResource TextMutedBrush}"/>
                                    <TextBlock Text="{Binding TeHours}" FontSize="12"
                                               Foreground="{DynamicResource TextBrush}"/>
                                </StackPanel>

                                <!-- Image-derived events (separate from user / /classes events) -->
                                <ItemsControl ItemsSource="{Binding TeEvents}"
                                              Visibility="{Binding HasTeEvents, Converter={StaticResource BoolToVisConverter}}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#5F7A5C" CornerRadius="6"
                                                    Padding="8,6" Margin="0,0,0,4">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold"
                                                               Foreground="White" TextWrapping="Wrap"/>
                                                    <TextBlock Text="{Binding Time}" FontSize="11"
                                                               Foreground="White" Opacity="0.9"
                                                               Visibility="{Binding Time, Converter={StaticResource NullToVisConverter}}"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource SuccessSoftBrush}" Padding="12,8" Margin="12,8,12,0" CornerRadius="6">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding StatusMessage}" Value="">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource SuccessBrush}" FontWeight="SemiBold" FontSize="12"/>
                        </Border>

                        <!-- No day selected placeholder -->
                        <TextBlock Text="Click any day to view or add events."
                                   Foreground="{DynamicResource TextMutedBrush}" FontSize="13"
                                   HorizontalAlignment="Center" TextAlignment="Center"
                                   TextWrapping="Wrap" Margin="20,48,20,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedDay}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- "No events" placeholder. Only when there are no user
                             events AND no TE content on the selected day; otherwise
                             one of the panels above already gives the user something
                             to look at. -->
                        <TextBlock Text="No events on this day. Click '+ Add Event' to create one."
                                   Foreground="{DynamicResource TextMutedBrush}" FontSize="13"
                                   HorizontalAlignment="Center" TextAlignment="Center"
                                   TextWrapping="Wrap" Margin="20,48,20,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding SelectedDayEvents.Count}" Value="0"/>
                                                <Condition Binding="{Binding SelectedDay.HasTeHours}"  Value="False"/>
                                                <Condition Binding="{Binding SelectedDay.HasTeStatus}" Value="False"/>
                                                <Condition Binding="{Binding SelectedDay.HasTeEvents}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        </StackPanel>

                        <!-- Events list -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding SelectedDayEvents}" Margin="12,8,12,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource CardBrush}"
                                                BorderBrush="{DynamicResource BorderBrush}"
                                                BorderThickness="1" CornerRadius="8"
                                                Margin="0,0,0,8" Padding="12,10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="6"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Color stripe -->
                                                <Border Grid.Column="0" CornerRadius="3" Margin="0,0,10,0"
                                                        Background="{DynamicResource PrimaryBrush}"/>

                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold"
                                                               FontSize="13" Foreground="{DynamicResource TextBrush}"
                                                               TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Text="{Binding DisplayTime}" FontSize="11"
                                                               Foreground="{DynamicResource TextMutedBrush}" Margin="0,2,0,0"/>
                                                    <TextBlock Text="{Binding Description}" FontSize="11"
                                                               Foreground="{DynamicResource TextMutedBrush}"
                                                               TextWrapping="Wrap" Margin="0,2,0,0" MaxWidth="180">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Description}" Value="{x:Null}">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>

                                                    <!-- Sign-up link for TE-sourced events (events with a registration URL). -->
                                                    <Button Content="Sign up at Taylored Expressions →"
                                                            Command="{Binding DataContext.OpenEventUrlCommand, RelativeSource={RelativeSource AncestorType=views:CalendarView}}"
                                                            CommandParameter="{Binding TeUrl}"
                                                            HorizontalAlignment="Left"
                                                            Cursor="Hand"
                                                            Padding="0" Margin="0,6,0,0"
                                                            Visibility="{Binding TeUrl, Converter={StaticResource NullToVisConverter}}">
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <TextBlock Text="{TemplateBinding Content}"
                                                                           Foreground="{DynamicResource PrimaryBrush}"
                                                                           FontSize="11" FontWeight="SemiBold"
                                                                           TextDecorations="Underline"/>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                    </Button>
                                                </StackPanel>

                                                <!-- Edit / Delete buttons -->
                                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top">
                                                    <Button Style="{StaticResource SecondaryButton}" Content="✏"
                                                            Command="{Binding DataContext.StartEditCommand, RelativeSource={RelativeSource AncestorType=views:CalendarView}}"
                                                            CommandParameter="{Binding}"
                                                            Width="28" Height="28" Padding="0" FontSize="11" Margin="0,0,4,0"/>
                                                    <Button Content="✕"
                                                            Command="{Binding DataContext.DeleteEventCommand, RelativeSource={RelativeSource AncestorType=views:CalendarView}}"
                                                            CommandParameter="{Binding}"
                                                            Width="28" Height="28" Padding="0" FontSize="11">
                                                        <Button.Style>
                                                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                                                <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>

                <!-- ═══════════════════════════════════════════════════════ -->
                <!-- ADD / EDIT FORM                                         -->
                <!-- ═══════════════════════════════════════════════════════ -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ScrollViewer.Style>
                        <Style TargetType="ScrollViewer">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAdding}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ScrollViewer.Style>
                    <StackPanel Margin="16">

                        <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,14">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Edit Event"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAdding}" Value="True">
                                            <Setter Property="Text" Value="New Event"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Error Message -->
                        <Border Background="{DynamicResource DangerSoftBrush}" Padding="10,7" CornerRadius="6" Margin="0,0,0,12">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource DangerBrush}" FontSize="12"/>
                        </Border>

                        <!-- Title -->
                        <TextBlock Text="Title *" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding EditTitle, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource SearchTextBox}" Padding="8,7" Margin="0,0,0,12"/>

                        <!-- Description -->
                        <TextBlock Text="Description" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource SearchTextBox}" Padding="8,7"
                                 Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top"
                                 Margin="0,0,0,12"/>

                        <!-- Date -->
                        <TextBlock Text="Date *" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <DatePicker SelectedDate="{Binding EditDate}" Margin="0,0,0,12"
                                    MinHeight="34" HorizontalAlignment="Stretch"
                                    VerticalContentAlignment="Center"/>

                        <!-- All Day toggle -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <CheckBox IsChecked="{Binding EditIsAllDay}" VerticalAlignment="Center"
                                      Margin="0,0,8,0"/>
                            <TextBlock Text="All Day Event" FontSize="13"
                                       Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Time picker (visible when not all-day) -->
                        <StackPanel Margin="0,0,0,12">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding EditIsAllDay}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="Time" FontSize="11" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,4"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding EditTimeHour, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource SearchTextBox}" Width="48" Padding="8,7"
                                         MaxLength="2"/>
                                <TextBlock Text=":" FontSize="16" VerticalAlignment="Center" Margin="4,0"/>
                                <TextBox Text="{Binding EditTimeMinute, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource SearchTextBox}" Width="48" Padding="8,7"
                                         MaxLength="2"/>
                                <ComboBox ItemsSource="{Binding AmPmOptions}"
                                          SelectedItem="{Binding EditTimeAmPm}"
                                          Width="56" Margin="6,0,0,0" VerticalContentAlignment="Center"
                                          Height="34"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Reminder -->
                        <TextBlock Text="Remind me" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding ReminderOptions}"
                                  SelectedItem="{Binding EditReminderLabel}"
                                  Height="34" VerticalContentAlignment="Center" Margin="0,0,0,12"/>

                        <!-- Color -->
                        <TextBlock Text="Color" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,6"/>
                        <ItemsControl ItemsSource="{Binding ColorOptions}" Margin="0,0,0,16">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="28" Height="28" CornerRadius="14" Margin="3" Cursor="Hand"
                                            Background="{Binding}">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick"
                                                          Command="{Binding DataContext.SetColorCommand, RelativeSource={RelativeSource AncestorType=views:CalendarView}}"
                                                          CommandParameter="{Binding}"/>
                                        </Border.InputBindings>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Save / Cancel -->
                        <StackPanel Orientation="Horizontal">
                            <Button Content="💾 Save" Command="{Binding SaveEventCommand}"
                                    Padding="14,8" Margin="0,0,8,0">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Style="{StaticResource SecondaryButton}" Content="Cancel"
                                    Command="{Binding CancelFormCommand}" Padding="14,8"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

                <!-- ═══════════════════════════════════════════════════════ -->
                <!-- Shop hours (scraped from the TE storefront homepage).   -->
                <!-- Always visible at the bottom of the sidebar so the user -->
                <!-- can see when the store is open regardless of what day   -->
                <!-- they have selected.                                     -->
                <!-- ═══════════════════════════════════════════════════════ -->
                <Border Grid.Row="1"
                        Background="{DynamicResource CardBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,1,0,0"
                        Padding="16,12"
                        Visibility="{Binding ShopHours.Count, Converter={StaticResource ZeroToVisConverter}, ConverterParameter=Inverse}">
                    <StackPanel>
                        <TextBlock Text="TE Shop hours" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,6"/>
                        <ItemsControl ItemsSource="{Binding ShopHours}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="84"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Day}"
                                                   FontSize="12" Foreground="{DynamicResource TextBrush}"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Text}"
                                                   FontSize="12" Foreground="{DynamicResource TextMutedBrush}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>

<Window x:Class="MyCraftyStash.Views.UpdateDownloadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Downloading update"
        Height="190" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText"
                   Text="Downloading My Crafty Stash update..."
                   FontWeight="SemiBold" FontSize="14"
                   Foreground="{DynamicResource TextBrush}"/>

        <ProgressBar Grid.Row="1" x:Name="DownloadProgress"
                     Height="10" Margin="0,14,0,8"
                     Minimum="0" Maximum="100" Value="0"/>

        <TextBlock Grid.Row="2" x:Name="StatusText"
                   Text="Starting download..."
                   FontSize="12"
                   Foreground="{DynamicResource TextMutedBrush}"/>

        <Button Grid.Row="4" x:Name="CancelButton"
                Content="Cancel" Padding="22,7"
                HorizontalAlignment="Right"
                Style="{StaticResource SecondaryButton}"
                Click="CancelButton_Click"/>
    </Grid>
</Window>

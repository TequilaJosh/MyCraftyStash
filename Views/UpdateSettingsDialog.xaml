<Window x:Class="MyCraftyStash.Views.UpdateSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Settings" 
        Height="220" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Configure Update Location"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1"
                   Text="Enter the network path where updates will be located:"
                   Foreground="{DynamicResource TextMutedBrush}"
                   Margin="0,0,0,8"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="UpdatePathTextBox"
                     Grid.Column="0"
                     Text="{Binding UpdatePath, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource SearchTextBox}"
                     Margin="0,0,8,0"/>

            <Button Grid.Column="1"
                    Content="Test Path"
                    Click="BrowseButton_Click"
                    Style="{StaticResource SecondaryButton}"
                    Padding="16,8"/>
        </Grid>

        <CheckBox Grid.Row="3"
                  x:Name="CheckOnStartupCheckBox"
                  Content="Check for updates on startup"
                  IsChecked="{Binding CheckForUpdatesOnStartup}"
                  Foreground="{DynamicResource TextBrush}"
                  VerticalAlignment="Top"
                  Margin="0,16,0,0"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Skip"
                    Click="SkipButton_Click"
                    Style="{StaticResource SecondaryButton}"
                    Padding="24,8"
                    Margin="0,0,8,0"/>
            <Button Content="Save"
                    Click="SaveButton_Click"
                    Style="{StaticResource PrimaryButton}"
                    Padding="24,8"/>
        </StackPanel>
    </Grid>
</Window>

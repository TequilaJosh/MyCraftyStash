<UserControl x:Class="MyCraftyStash.Views.SystemColorMatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="20,16,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / search -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextBrush}">
                    <Run Text="{Binding DisplayName, Mode=OneWay}"/><Run Text=" → TE Color Match"/>
                </TextBlock>
                <TextBlock Margin="0,2,0,0" FontSize="12"
                           Foreground="{DynamicResource TextMutedBrush}"
                           Text="Hover any row to see what to order. ✓ marks colors already in your stash."/>
                <TextBlock Margin="0,4,0,0" FontSize="12" FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryBrush}"
                           Text="{Binding SummaryText, Mode=OneWay}"/>
            </StackPanel>

            <TextBox Grid.Column="1" Width="220" Height="32" VerticalContentAlignment="Center"
                     Padding="10,0" FontSize="13"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Background="{DynamicResource CardBrush}"
                     Foreground="{DynamicResource TextBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"/>
        </Grid>

        <!-- Filter chips: All / Owned / Missing -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <RadioButton GroupName="ColorFilter"
                         IsChecked="{Binding ShowAll, Mode=TwoWay}"
                         Style="{StaticResource {x:Type ToggleButton}}"
                         Margin="0,0,8,0" Padding="14,6" Cursor="Hand"
                         Background="{DynamicResource CardBrush}"
                         Foreground="{DynamicResource TextBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         Content="All"/>
            <RadioButton GroupName="ColorFilter"
                         IsChecked="{Binding ShowOwned, Mode=TwoWay}"
                         Style="{StaticResource {x:Type ToggleButton}}"
                         Margin="0,0,8,0" Padding="14,6" Cursor="Hand"
                         Background="{DynamicResource CardBrush}"
                         Foreground="{DynamicResource TextBrush}"
                         BorderBrush="{DynamicResource BorderBrush}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="✓ Owned" FontWeight="SemiBold"/>
                </StackPanel>
            </RadioButton>
            <RadioButton GroupName="ColorFilter"
                         IsChecked="{Binding ShowMissing, Mode=TwoWay}"
                         Style="{StaticResource {x:Type ToggleButton}}"
                         Margin="0,0,8,0" Padding="14,6" Cursor="Hand"
                         Background="{DynamicResource CardBrush}"
                         Foreground="{DynamicResource TextBrush}"
                         BorderBrush="{DynamicResource BorderBrush}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="○ Missing" FontWeight="SemiBold"/>
                </StackPanel>
            </RadioButton>

            <Button Margin="12,0,0,0" Padding="10,6" Cursor="Hand"
                    Background="Transparent"
                    Foreground="{DynamicResource TextMutedBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    Command="{Binding RefreshCommand}"
                    ToolTip="Re-check inventory and refresh ownership status."
                    Content="↻ Refresh"/>

            <Button Margin="6,0,0,0" Padding="10,6" Cursor="Hand"
                    Background="Transparent"
                    Foreground="{DynamicResource TextMutedBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    Command="{Binding ReseedCommand}"
                    ToolTip="Wipe this system's mappings and reload them from the built-in defaults. Use only if the table is stuck empty or corrupt."
                    Content="⟳ Re-seed defaults"/>
        </StackPanel>

        <!-- Match table -->
        <Border Grid.Row="2" Background="{DynamicResource CardBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                CornerRadius="8">
            <Grid>
                <!-- Empty-state diagnostic. Sits behind the DataGrid so it
                     only shows through when the grid has zero rows. -->
                <Border Background="#FFFFF4D6" BorderBrush="#FFE0B341"
                        BorderThickness="1" CornerRadius="6"
                        Margin="20" Padding="16,14"
                        VerticalAlignment="Top"
                        Visibility="{Binding ShowDiagnostic, Converter={StaticResource BoolToVisConverter}}">
                    <StackPanel>
                        <TextBlock Text="No mappings to show" FontWeight="SemiBold" FontSize="13"
                                   Foreground="#FF8A5A00" Margin="0,0,0,4"/>
                        <TextBlock Text="If the All filter is empty too, the seed didn't land. Try ↻ Refresh first; if that doesn't help, ⟳ Re-seed defaults wipes this system and re-imports the built-in mappings."
                                   FontSize="12" TextWrapping="Wrap" Foreground="#FF6B4500"/>
                        <TextBlock Margin="0,8,0,0" FontSize="11" FontFamily="Consolas"
                                   TextWrapping="Wrap"
                                   Foreground="#FF6B4500"
                                   Text="{Binding DiagnosticText}"/>
                    </StackPanel>
                </Border>

                <DataGrid ItemsSource="{Binding Filtered}"
                      SelectedItem="{Binding Selected, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      RowHeight="34"
                      Background="Transparent"
                      RowBackground="Transparent"
                      AlternatingRowBackground="{DynamicResource BackgroundBrush}"
                      BorderThickness="0"
                      Foreground="{DynamicResource TextBrush}"
                      HorizontalGridLinesBrush="{DynamicResource BorderBrush}">
                <!-- Per-row hover tooltip: "Order DMC 902 to match Mulled Wine." -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="ToolTip" Value="{Binding HoverHint}"/>
                        <Setter Property="ToolTipService.InitialShowDelay" Value="350"/>
                        <Setter Property="ToolTipService.ShowDuration" Value="8000"/>
                        <Style.Triggers>
                            <!-- Owned rows get a subtle green tint so the
                                 page reads as a quick visual completeness map. -->
                            <DataTrigger Binding="{Binding IsOwned}" Value="True">
                                <Setter Property="Background" Value="#225F7A5C"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource SidebarBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Owned" Binding="{Binding OwnedGlyph}" Width="64">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsOwned}" Value="True">
                                        <Setter Property="Foreground" Value="#5F7A5C"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Code" Binding="{Binding ExternalCode}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="12,0"/>
                                <Setter Property="FontFamily" Value="Consolas"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Taylored Expressions Color" Binding="{Binding TeColorName}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="12,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="200">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="12,0"/>
                                <Setter Property="FontStyle" Value="Italic"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
